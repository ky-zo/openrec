# OpenRec Build Configuration
# Copy this file to .env.build and fill in your values

# Notarization profile name (stored via: xcrun notarytool store-credentials)
export NOTARY_PROFILE="openrec-notary"

# Code signing identity (auto-detected if not set)
# export SIGN_ID="Developer ID Application: YOUR NAME (TEAM_ID)"
